---
// Hero section with value proposition
---

<section class="relative min-h-screen flex items-center justify-center px-4 py-20 overflow-hidden">
  <!-- Background gradient -->
  <div class="absolute inset-0 bg-gradient-to-b from-black via-gray-900 to-black"></div>

  <!-- Animated background elements -->
  <div class="absolute inset-0 opacity-30">
    <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-[#d4ff2a]/20 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-purple-500/20 rounded-full blur-3xl animate-pulse delay-1000"></div>
  </div>

  <div class="relative max-w-6xl mx-auto text-center z-10">
    <!-- Logo -->
    <div class="mb-8 flex justify-center">
      <img
        src="/logo.png"
        alt="hilm.ai logo"
        class="w-20 h-20 transform hover:scale-110 transition-transform"
      />
    </div>

    <!-- Main Heading -->
    <h1 class="text-5xl md:text-7xl lg:text-8xl font-bold mb-8 leading-tight">
      Your AI Personal
      <br />
      <span class="bg-gradient-to-r from-[#d4ff2a] to-green-400 bg-clip-text text-transparent">
        Finance Assistant
      </span>
    </h1>

    <!-- Subheading -->
    <p class="text-xl md:text-2xl text-gray-400 mb-12 max-w-3xl mx-auto leading-relaxed">
      Chat with your money, get smart insights. Send expenses via text, voice, or receipt photos—get instant insights and tailored advice.
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-16">
      <a
        href="https://t.me/hilmaibot"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 px-8 py-4 bg-[#d4ff2a] text-black font-semibold rounded-xl hover:bg-[#c5f01a] transform hover:scale-105 transition-all shadow-lg shadow-[#d4ff2a]/20 group"
      >
        <span>Try Now</span>
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" class="group-hover:translate-x-1 transition-transform">
          <path d="M5 12h14M12 5l7 7-7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
      <a
        href="/pricing"
        id="view-pricing-btn"
        class="px-8 py-4 bg-gray-800/50 text-white font-semibold rounded-xl border border-gray-700 hover:bg-gray-800 hover:border-[#d4ff2a]/50 transition-all backdrop-blur-sm"
      >
        View Pricing
      </a>
    </div>

    <!-- Status Badge -->
    <div class="text-center">
      <p class="text-gray-400 text-sm">
        Available now on <span class="text-[#d4ff2a] font-semibold">Telegram</span> • Start your 7-day free trial
      </p>
    </div>
  </div>
</section>

<script>
  import { conversionEvents } from '../lib/analytics';

  // Track View Pricing button click
  const viewPricingBtn = document.getElementById('view-pricing-btn');
  if (viewPricingBtn) {
    viewPricingBtn.addEventListener('click', () => {
      conversionEvents.clickViewPricing();
    });
  }
</script>

<style>
  @keyframes pulse {
    0%, 100% {
      opacity: 0.3;
    }
    50% {
      opacity: 0.5;
    }
  }

  .animate-pulse {
    animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  .delay-1000 {
    animation-delay: 1s;
  }
</style>
