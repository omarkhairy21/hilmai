---
// FAQ Section with accordion
const faqs = [
  {
    question: 'What types of inputs does hilm.ai support?',
    answer: 'You can track expenses using text messages (e.g., "Coffee $5"), voice notes, or receipt photos. Our AI processes all formats automatically.'
  },
  {
    question: 'Is my financial data secure?',
    answer: 'Yes. All data is encrypted and stored securely. We never share your information with third parties, and you can delete your data anytime.'
  },
  {
    question: 'What kind of AI advice do I get?',
    answer: 'Based on your spending patterns, you will receive personalized insights like identifying spending trends, suggesting savings opportunities, and detailed financial analysis.'
  },
  {
    question: 'When will hilm.ai launch?',
    answer: 'We are currently in development. Join the waitlist to be among the first to get early access when we launch.'
  },
  {
    question: 'Is it free?',
    answer: 'You get 5 free messages to try hilm.ai with no credit card required. After that, you can subscribe to our Pro plan or start a 7-day free trial with full access. View our pricing page for more details.'
  }
];
---

<section class="py-20 px-4">
  <div class="max-w-3xl mx-auto">
    <!-- Section Header -->
    <div class="text-center mb-12">
      <h2 class="text-4xl md:text-5xl font-bold mb-4">
        Frequently Asked Questions
      </h2>
      <p class="text-lg text-gray-400">
        Everything you need to know about hilm.ai. Find answers to the most common questions below.
      </p>
    </div>

    <!-- FAQ Accordion -->
    <div class="space-y-4">
      {faqs.map((faq, index) => (
        <div class="border border-gray-800 rounded-xl overflow-hidden">
          <button
            class="faq-question w-full px-6 py-5 text-left flex items-center justify-between hover:bg-gray-900/50 transition-colors"
            data-index={index}
          >
            <span class="font-medium text-lg">{faq.question}</span>
            <svg
              class="faq-icon w-6 h-6 transition-transform flex-shrink-0 ml-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
            </svg>
          </button>
          <div class="faq-answer max-h-0 overflow-hidden transition-all duration-300">
            <div class="px-6 pb-5 text-gray-400 leading-relaxed">
              {faq.answer}
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  const questions = document.querySelectorAll('.faq-question');

  questions.forEach((question) => {
    question.addEventListener('click', () => {
      const answer = question.nextElementSibling as HTMLElement;
      const icon = question.querySelector('.faq-icon') as HTMLElement;
      const isOpen = answer.style.maxHeight && answer.style.maxHeight !== '0px';

      // Close all other FAQs
      document.querySelectorAll('.faq-answer').forEach((item) => {
        (item as HTMLElement).style.maxHeight = '0';
      });
      document.querySelectorAll('.faq-icon').forEach((item) => {
        (item as HTMLElement).style.transform = 'rotate(0deg)';
      });

      // Toggle current FAQ
      if (!isOpen) {
        answer.style.maxHeight = answer.scrollHeight + 'px';
        icon.style.transform = 'rotate(45deg)';
      }
    });
  });
</script>

<style>
  .faq-icon {
    color: #d4ff2a;
  }
</style>
